yolo:
  imgsz: 224
  fliplr: 0.5
  flipud: 0.1
  hsv_h: 0.02
  hsv_s: 0.4
  hsv_v: 0.4
  erasing: 0.2
  workers: 4
  epochs: 30
  batch: 32

siglip2:
  dtype: float16
  attn_implementation: sdpa
  batch_size: 12
  prompt_template: "This photo contains: {}."

llava_next:
  dtype: bfloat16
  batch_size: 8
  tensor_parallel_size: 1
  gpu_memory_utilization: 0.90
  max_model_len: 8192

  # prompt_template: "What is shown in this image? Choose one: {classes}. Answer with only the class name."
  # prompt_template: "Respond in one sentence: What disease, pest, damage type, or other stress, if any, is exhibited in this image of a {plant_type} plant?"
  # prompt_template: "Respond in one sentence: What plant or weed species, if any, is shown in this image?"
  prompt_template: "Classify this image into one of the following categories: {classes}. Respond with ONLY the category name, nothing else."

  mcqa_options:
    options_within_dataset: True
    mcqa_num_choices: 5

qwen_vl:
  dtype: bfloat16
  batch_size: 1
  tensor_parallel_size: 1
  gpu_memory_utilization: 0.90

  # Pixel constraints for Qwen VL's dynamic tiling (height * width).
  # 448*448 = 200704 fixes resolution to ~1024 visual tokens per image.
  # Set to null to use processor defaults (dynamic resolution).
  min_pixels: 200704  # 448 * 448
  max_pixels: 200704  # 448 * 448

  # prompt_template: "What is shown in this image? Choose one: {classes}. Answer with only the class name."
  # prompt_template: "Respond in one sentence: What disease, pest, damage type, or other stress, if any, is exhibited in this image of a {plant_type} plant?"
  # prompt_template: "Respond in one sentence: What plant or weed species, if any, is shown in this image?"
  # prompt_template: "Classify this image into one of the following categories: {classes}. Respond with ONLY the category name, nothing else."
  prompt_template: "Classify this image of a {plant_type} plant into one of the following categories: {classes}. Respond with ONLY the category name, nothing else."

  mcqa_options:
    options_within_dataset: True
    mcqa_num_choices: 5

  context_options:
    max_num_context: 1
    max_num_example: 1

qwen_vl_72b:
  dtype: bfloat16
  batch_size: 1
  tensor_parallel_size: 1
  gpu_memory_utilization: 0.90

  # prompt_template: "What is shown in this image? Choose one: {classes}. Answer with only the class name."
  # prompt_template: "Classify this image into one of the following categories: {classes}. Respond with ONLY the category name, nothing else."
  prompt_template: "Classify this image of a {plant_type} plant into one of the following categories: {classes}. Respond with ONLY the category name, nothing else."

  mcqa_options:
    options_within_dataset: True
    mcqa_num_choices: 5

  sample_limit: 0.1

qwen_vl_3:
  dtype: bfloat16
  batch_size: 1
  tensor_parallel_size: 1
  gpu_memory_utilization: 0.90

  min_pixels: 200704  # 448 * 448
  max_pixels: 200704  # 448 * 448

  # prompt_template: "What is shown in this image? Choose one: {classes}. Answer with only the class name."
  # prompt_template: "Classify this image into one of the following categories: {classes}. Respond with ONLY the category name, nothing else."
  prompt_template: "Classify this image of a {plant_type} plant into one of the following categories: {classes}. Respond with ONLY the category name, nothing else."

  mcqa_options:
    options_within_dataset: True
    mcqa_num_choices: 5

  context_options:
    max_num_context: 1
    max_num_example: 1

gemma_3:
  dtype: bfloat16
  batch_size: 8
  tensor_parallel_size: 1
  gpu_memory_utilization: 0.90

  min_pixels: 200704  # 448 * 448
  max_pixels: 200704  # 448 * 448

  # prompt_template: "Respond in one sentence: What disease, pest, damage type, or other stress, if any, is exhibited in this image of a {plant_type} plant?"
  # prompt_template: "What plant or weed species, if any, is shown in this image?"
  prompt_template: "What is shown in this image? Choose one: {classes}. Answer with only the class name."

deepseek_vl:
  dtype: bfloat16
  batch_size: 8
  tensor_parallel_size: 1
  gpu_memory_utilization: 0.90
  trust_remote_code: true

  # prompt_template: "<image_placeholder>What disease, pest, damage type, or other stress, if any, is exhibited in this image of a {plant_type} plant?"
  # prompt_template: "<image_placeholder>Respond in one sentence: What plant or weed species, if any, is shown in this image?"
  prompt_template: "<image_placeholder>What is shown in this image? Choose one: {classes}. Answer with only the class name."

gpt-5-nano:
  batch_size: 1
  max_workers: 10
  # prompt_template: "Classify this image into one of the following categories: {classes}. Respond with ONLY the category name, nothing else."
  # prompt_template: "Respond in one sentence: What disease, pest, damage type, or other stress, if any, is exhibited in this image of a {plant_type} plant?"
  prompt_template: "Respond in one sentence: What plant or weed species, if any, is shown in this image?"

gpt-5:
  sample_limit: 0.1
  batch_size: 1
  max_workers: 10
  # prompt_template: "Classify this image into one of the following categories: {classes}. Respond with ONLY the category name, nothing else."
  # prompt_template: "Respond in one sentence: What disease, pest, damage type, or other stress, if any, is exhibited in this image of a {plant_type} plant?"
  prompt_template: "Respond in one sentence: What plant or weed species, if any, is shown in this image?"

gemini-3-pro-preview:
  batch_size: 1
  sample_limit: 0.1
  use_batch: False
  batch_poll_interval: 60
  batch_max_wait: 86400
  max_workers: 10

  prompt_template: "Classify this image into one of the following categories: {classes}. Respond with ONLY the category name, nothing else."
  # prompt_template: "Respond in one sentence: What disease, pest, damage type, or other stress, if any, is exhibited in this image of a {plant_type} plant?"
  # prompt_template: "Respond in one sentence: What plant or weed species, if any, is shown in this image?"

claude-haiku-4-5:
  batch_size: 1
  max_workers: 10
  sample_limit: 0.1

  prompt_template: "Classify this image into one of the following categories: {classes}. Respond with ONLY the category name, nothing else."
  # prompt_template: "Respond in one sentence: What disease, pest, damage type, or other stress, if any, is exhibited in this image of a {plant_type} plant?"
  # prompt_template: "Respond in one sentence: What plant or weed species, if any, is shown in this image?"
